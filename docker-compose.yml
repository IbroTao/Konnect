version: "3.8"
services:
  redis:
    image: redis
    ports:
      - "6380:6379"
  mongodb:
    image: mongo:4
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
    driver: local
